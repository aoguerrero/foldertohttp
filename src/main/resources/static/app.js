'use strict';

angular
  .module('foldertohttpApp', ['ui.router'])
  .config([$stateProvider,
    function ($stateProvider) {
    }]).run(['$rootScope', '$http', '$location', function ($rootScope, $http, $location) {
    }]);

